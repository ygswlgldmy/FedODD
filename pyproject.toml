# =====================================================================
# For a full TOML configuration guide, check the Flower docs:
# https://flower.ai/docs/framework/how-to-configure-pyproject-toml.html
# =====================================================================

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fl"
version = "1.0.0"
description = ""
license = "Apache-2.0"
# Dependencies for your Flower App
dependencies = [
    "flwr[simulation]>=1.24.0",
    "flwr-datasets[vision]>=0.5.0",
    "torch>=2.7.1",
    "torchvision>=0.22.1",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.flwr.app]
publisher = "xiong"

# Point to your ServerApp and ClientApp objects
[tool.flwr.app.components]
serverapp = "fl.server_app:app"
clientapp = "fl.client_app:app"

# Custom config values accessible via `context.run_config`
[tool.flwr.app.config]
num-server-rounds = 50
fraction-train = 1.0   # 5个客户端全参与；0.1时每轮只有1个客户端，退化成顺序训练
local-epochs = 3       # RT-DETR每轮本地epoch不宜过多，防止客户端漂移
lr = 0.0001

# Default federation to use when running the app
[tool.flwr.federations]
default = "local-simulation"

# Local simulation federation with 5 virtual SuperNodes
# num-gpus 控制并发数：并发数 = floor(1.0 / num-gpus)
#   0.25 → 4并发（RT-DETR-L大概率OOM）
#   0.5  → 2并发（显存充裕时用）
#   1.0  → 1并发（严格顺序，最省显存，5个客户端依次跑）
[tool.flwr.federations.local-simulation]
options.num-supernodes = 5
options.backend.client-resources.num-cpus = 2
options.backend.client-resources.num-gpus = 1.0  # 顺序执行，每次只有1个client占用GPU

# Remote federation example for use with SuperLink
[tool.flwr.federations.remote-federation]
address = "<SUPERLINK-ADDRESS>:<PORT>"
insecure = true  # Remove this line to enable TLS
# root-certificates = "<PATH/TO/ca.crt>"  # For TLS setup
